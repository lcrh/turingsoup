# Rust / WASM build artifacts
wasm/target/
# Note: wasm/pkg/ is NOT ignored - it contains the built WASM needed at runtime
# Note: Cargo.lock is committed for reproducible builds

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node (if we add npm later)
node_modules/
